#ifndef CONCAT_H
#define CONCAT_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char *concat2(const char *a, const char *b);
char *concat(const char *a, const char *b, const char *c);

#endif
